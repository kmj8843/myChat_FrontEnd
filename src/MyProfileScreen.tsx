import React from 'react'
import { Text, View } from 'react-native';

const MyProfileScreen = () => {
  return (
    <View style = {{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
      <Text>Prfile Screen</Text>
    </View>
  );
}

export default MyProfileScreen;